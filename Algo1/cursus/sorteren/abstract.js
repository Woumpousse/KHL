function sorteer( rij ) {
    // Hier hoort de code voor een sorteeralgoritme
}

// Ongesorteerde rij aanmaken
var rij = [6, 2, 1, 4, 8, 7, 3, 9, 5];

// Sorteer de rij
sorteer( rij );

// Indien de functie 'sorteer' zijn werk doet,
// moet dit 1, 2, 3, 4, 5, 6, 7, 8, 9 afdrukken
alert( rij );
