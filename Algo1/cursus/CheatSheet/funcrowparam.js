function functie(rij) {
    rij.push(waarde4);
    // Pas op! De rij die je als parameter
    // hebt meegegeven werd nu
    // aangepast!
    return rij.length;
    // de nieuwe lengte van de rij wordt
    // terug gegeven
}
