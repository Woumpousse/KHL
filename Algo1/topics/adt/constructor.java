class Tijdstip {
    /* ... */

    public Tijdstip(int h,
                    int m,
                    int s) {
        `\NODE{setUur}{set hour}`(h);
        `\NODE{setMinuten}{set minutes}`(m);
        `\NODE{setSeconden}{set seconds}`(s);
    }

    /* ... */
}
